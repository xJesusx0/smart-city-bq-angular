/**
 * Environment configuration – PRODUCTION
 *
 * These values should be replaced with production values
 * before deploying (e.g., via CI/CD pipeline or manually).
 */
export const environment = {
  production: true,
  apiUrl: 'https://smart-city-bq-traffic-api.vercel.app',
  googleClientId: '',
  msalClientId: '',
  msalTenantId: '',
};
